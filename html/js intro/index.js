// oneline: cach nhau boi dau ,

// OPERATORS

// truth.lower()-> truth.toLowerCase()
// truth.upper()-> truth.toUpperCase()
// "truth".capitalize()-> Truth
// x+=1 -> x++
// x-=1 -> x--


// x=4.5
//Math.floor(Var): lam tron xuong, Math.ceil(var): lam tron len
// Math.abs(var): tri tuyet doi
// Math.sqrt(var): can bac 2
// Math.min/max

// var b1 = true; !b1--> false
// b1 and b2 (python) --> b1 && b2 (JS)
// b1 or b2(py) --> b1 || b2 (JS)

// list: var a = []
// add: a.push("something")
// delete: a.splice(index_start, so phan tu tinh tu phan muon xoa)
// delete by value: 
    // var i = a.indexOf("value")
    // a.slice(i,1)
// console.log("something") or alert = print

// loop
// while va for trong javascript co the thay the lan nhau

    // var i = 0;
    // while(i<3) {
    //     console.log("hi");
    //     i+=1;
    // }

    // for(var i = 0; i<3; i++){

    // }
// len(arr)--> arr.length
// for (var i =0;i<arr.length;i++){

// }
// loop stop --> break